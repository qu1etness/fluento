import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Dictionary",
  description: '...',
}

const Page = () => {
  return (
    <div>
    </div>
  );
};

export default Page;
